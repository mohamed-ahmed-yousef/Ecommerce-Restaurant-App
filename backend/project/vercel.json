{
    "builds": [{
        "src": "backend/project/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "backend/project/wsgi.py"
        }
    ]
}